# 无障碍验收

- 所有表单有明确 label、帮助文本和错误关联；
- 进度状态使用 `aria-live=polite`，错误使用适当 alert；
- Dialog/Drawer 锁定焦点并可 Esc 关闭；
- 图表提供表格或摘要；
- 颜色不是唯一编码；
- 时间轴可用键盘左右/上下选择；
- 标题层级连续；
- 主内容有 skip link；
- 触控目标不小于 44×44 CSS px；
- 200% 缩放仍可完成核心流程；
- 尊重减少动画偏好。
