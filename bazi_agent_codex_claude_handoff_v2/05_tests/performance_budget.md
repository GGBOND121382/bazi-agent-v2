# 前端性能预算

## 初始预算

- 首屏核心 JS gzip：目标 ≤ 220 KB，不含按需加载图表；
- 首屏 CSS gzip：目标 ≤ 60 KB；
- ECharts 和报告专业组件按路由懒加载；
- 命盘总览 LCP 在常规宽带和中档设备实验条件下目标 < 2.5s；
- 路由切换不阻塞主线程长任务 > 200ms；
- 长报告采用分节渲染，不一次挂载全部重型图表；
- 历史列表分页或游标加载。

预算是发布 Gate，超出必须记录原因和优化计划。
