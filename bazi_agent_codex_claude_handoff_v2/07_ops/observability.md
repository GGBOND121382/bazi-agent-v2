# 可观测性

## 后端

- request_id、job_id、chart_id 的不可逆/内部标识；
- 阶段耗时、模型 token、检索数量、验证结果；
- 不记录完整出生信息、地点、报告正文和 API key。

## 前端

- 页面加载、API 错误码、任务阶段失败、导出失败；
- 禁止发送表单值、命盘数据、报告内容和分享 token；
- Web Vitals 按路由聚合；
- E2E 与生产遥测使用不同项目。
