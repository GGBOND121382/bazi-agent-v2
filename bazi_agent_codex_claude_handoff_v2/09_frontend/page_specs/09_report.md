# 综合报告

## 桌面

左侧固定目录，中间正文，右侧可选证据抽屉。

## 移动

目录按钮 + 单列正文 + bottom sheet 证据。

## 功能

- 简洁/专业模式；
- 展开规则与引用；
- 打印/PDF；
- 重新生成指定章节（后续）；
- 复制不含敏感信息的摘要；
- 分享默认关闭。

ReportRenderer 必须按 block_type 白名单渲染，不直接显示模型 HTML/Markdown。
