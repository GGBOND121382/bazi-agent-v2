# 无障碍设计

- 使用语义 HTML；
- 页面只有一个主 H1；
- 路由切换后更新 title 并移动焦点到主标题；
- 表单错误在字段旁显示并汇总到顶部；
- 颜色之外使用文字和图标；
- 五行图提供可排序表格；
- 大运时间轴使用 listbox 或 tabs 语义，提供键盘操作说明；
- Drawer/Dialog 正确管理焦点；
- 进度事件用 `aria-live=polite`，不要频繁播报百分比；
- 支持系统高对比和减少动画偏好。
