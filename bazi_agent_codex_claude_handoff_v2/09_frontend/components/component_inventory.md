# 组件清单

## 基础 UI

Button、IconButton、Input、Select、Combobox、DateTimeField、Checkbox、RadioGroup、Tabs、Tooltip、Popover、Dialog、Drawer、BottomSheet、Toast、Alert、Skeleton、EmptyState、ErrorState、Badge、DataTable、Pagination。

## 命理领域组件

- `PillarCard`：天干、地支、十神、藏干、纳音、长生；
- `FourPillarsGrid`；
- `HiddenStemList`；
- `TenGodBadge`；
- `ElementLegend`；
- `FiveElementChart` + `FiveElementTable`；
- `RelationshipChip` / `RelationshipGraph`；
- `BoundaryRiskCard`；
- `CandidateChartCompare`；
- `QiyunSummary`；
- `DayunTimeline`；
- `YearSelector`；
- `MonthCalendar`；
- `TemporalBreadcrumb`；
- `ShenshaList`；
- `ClaimCard`；
- `EvidenceDrawer`；
- `CalculationProfileSheet`；
- `AnalysisStageStepper`；
- `ReportRenderer`；
- `PrintHeader/Footer`。

每个领域组件必须注明“只显示后端事实”或“显示验证后的模型 claim”。
