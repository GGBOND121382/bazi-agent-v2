# 隐私、删除与恢复演练

- 普通日志只记录 request/job/trace ID、阶段、耗时、错误码；禁止生日、地点、API key 和 Provider 原文。
- 命盘删除为软删除，默认列表不可见；正式部署由后台保留策略异步清除派生分析、报告与分享。
- 分享 token 只在创建时返回一次，存储 SHA-256 hash；默认关闭、最长 30 天、可立即撤销。
- 备份必须加密、访问审计、与生产凭据分离。每次发布前在隔离环境验证版本化备份 envelope。
- 恢复演练必须验证：记录数、命盘/报告关联、已撤销分享仍不可用、评测集未混入生产数据。
